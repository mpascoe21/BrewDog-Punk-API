import { cleanup, render, screen } from '@testing-library/react';
import CardList from './CardList';

afterEach(cleanup)

// const addBeer = (beers) => {
//   beers.forEach(beer => {

//   })
// }

describe('CardList', () => {
  it('should render beercards', async () => {
    //render(<CardList />);
    // const {getByTestId} = render(<CardList />);
    // const beerCardElement = await screen.findByTestId(/beerContainer/i);
    // expect(beerCardElement).toBeInTheDocument();
  });
})